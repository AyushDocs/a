import React from 'react';
export default function About(props) {  
return (
    <div>    
    {/* {props.textAndHeadings
        .map((element)=>{return(
        <AboutItem heading={element.heading} text1={element.text1} text2={element.text2}/>
        )})} */}
    </div>)}